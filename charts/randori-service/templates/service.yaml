apiVersion: v1
kind: Service
metadata:
  name: {{ include "randori-service.name" . }}
  labels:
    app: {{ include "randori-service.name" . }}
    chart: {{ include "randori-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "randori-service.name" . }}
    release: {{ .Release.Name }}
