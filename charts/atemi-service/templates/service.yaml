apiVersion: v1
kind: Service
metadata:
  name: {{ include "atemi-service.name" . }}
  labels:
    app: {{ include "atemi-service.name" . }}
    chart: {{ include "atemi-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "atemi-service.name" . }}
    release: {{ .Release.Name }}
