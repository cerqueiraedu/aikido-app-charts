replicaCount: 3

image:
  repository: ecerqueira/atemi-service
  tag: "3.1.0"
  pullPolicy: Always

nameOverride: ""
fullnameOverride: ""
secretName: "this is a secret"

service:
  type: LoadBalancer
  port: 8080

ingress:  
  annotations: 
    nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: "nginx"
  enabled: true
  path: /aikido/uke
  servicePort: 8080
  hosts:
    - atemi-service.local